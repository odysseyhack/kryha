{
  "name": "ethereum",
  "version": "0.0.1",
  "description": "Ethereum smart contracts stuff",
  "scripts": {
    "start": "npm run migrate && babel ./src --out-dir ./build && NODE_ENV=production node ./build",
    "migrate": "truffle migrate",
    "dev": "nodemon ./src --exec babel-node",
    "test": "jest ./src"
  },
  "dependencies": {
    "@babel/cli": "^7.4.3",
    "@babel/core": "^7.4.3",
    "@babel/node": "^7.2.2",
    "@babel/preset-env": "^7.4.3",
    "cors": "^2.8.5",
    "express": "^4.16.4",
    "mongoose": "^5.5.1",
    "truffle": "^5.0.1"
  },
  "devDependencies": {
    "jest": "^24.7.1",
    "nodemon": "^1.18.11",
    "standard": "^12.0.1"
  }
}
